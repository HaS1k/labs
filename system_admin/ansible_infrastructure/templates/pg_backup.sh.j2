#!/usr/bin/env bash
set -euo pipefail
BACKUP_DIR="{{ postgresql_backup_dir }}"
DB_NAME="{{ postgresql_database }}"
mkdir -p "$BACKUP_DIR"
pg_dump "$DB_NAME" | gzip > "$BACKUP_DIR/${DB_NAME}_$(date +%F_%H%M%S).sql.gz"
find "$BACKUP_DIR" -type f -name "${DB_NAME}_*.sql.gz" -mtime +7 -delete
